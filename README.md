# Мой workflow с Claude Code: Как я пишу в нем код

Я организовал работу с Claude Code через структуру папок и Git Worktrees. Это позволяет писать код быстрее и избегать ошибок нейросети.

---

## 1. Структура папок

Воспринимайте нейросеть как верного, но зажатого сотрудника — она боится сказать, что что-то не ясно. Если на нейросеть накинуть тонну кода и текст задачи, она может запутаться и сделать совсем не то, что нужно. Я создаю для неё рабочую среду.

Структура папок выглядит так:

```
📂 Work/                    # из этой папки я запускаю claude code
├── 📂 Project/             # сам код
├── 📂 Worktrees/           # для параллельной работы, об этом ниже
├── 📂 Tasks/               # менеджмент задач
│   ├── 📂 todo/            # новые задачи
│   ├── 📂 in-progress/     # в работе
│   └── 📂 done/            # завершённые
└── 📂 Docs/                # база знаний проекта (wiki)
```

**Сначала наполните папки Project и Docs.**

### Пример файла задачи

Создайте файл `tasks/todo/add-logout-button.md`:

```markdown
# Задача: Добавить кнопку выхода из аккаунта

## Описание
Добавить кнопку "Выйти" в хедер для авторизованных пользователей.

## Требования
- Кнопка видна только авторизованным
- При клике — выход и редирект на главную
- Очистка токена из localStorage

## Критерии приёмки
- [ ] Кнопка работает
- [ ] Тесты написаны
```

> Полный пример: [`tasks/todo/example-task.md`](tasks/todo/example-task.md)

---

## 2. Анализ проекта

Прежде чем дать задачу, я убеждаюсь, что ИИ понимает проект.

- Я прошу Claude Code просканировать весь проект
- Он сопоставляет код с моей документацией в папке Docs
- Если чего-то не хватает, он сам дополняет документацию

### Команда для анализа:

```
/init-project
```

Или вручную:

```
Просканируй проект в папке Project/.
Изучи структуру, основные модули, используемые технологии.
Сопоставь код с документацией в папке Docs/.
Если что-то различается — скажи что именно.
```

После выполняю команду `/init` для инициализации файла `CLAUDE.md` с инструкциями для нейросети. Это главный промпт Claude Code, и важно его перечитать и дополнить.

> Шаблон: [`CLAUDE.md`](CLAUDE.md)

---

## 3. Постановка задачи

Теперь можно выполнить задачу. Когда появляется задача, я копирую её описание (ТЗ, комментарии) в папку `tasks/todo/` и прошу ИИ проверить ТЗ на ошибки и неясности.

### Команда для анализа задачи:

```
/analyze-task add-logout-button
```

Что делает команда:
1. Читает описание задачи
2. Изучает связанный код в проекте
3. Выписывает список вопросов и неясностей
4. Предлагает план реализации
5. Сохраняет анализ в `tasks/todo/add-logout-button-analysis.md`

> Полный код команды: [`.claude/commands/analyze-task.md`](.claude/commands/analyze-task.md)

Со списком вопросов иду к аналитику, отвечаю, и возвращаю ответы. **Цикл повторяется, пока у ИИ не останется вопросов.** Только тогда мы приступаем к коду.

> В моём проекте так ИИ нашёл десятки несостыковок в ТЗ.

### Команда для реализации:

```
/implement add-logout-button
```

Что делает команда:
1. Читает задачу и анализ
2. Создаёт ветку `feature/add-logout-button`
3. Пишет код согласно плану
4. Проверяет что всё работает
5. Пишет тесты
6. Перемещает задачу в `tasks/done/`

> Полный код команды: [`.claude/commands/implement.md`](.claude/commands/implement.md)

### Команда для code review:

```
/review
```

> Полный код команды: [`.claude/commands/review.md`](.claude/commands/review.md)

---

## 4. Масштабирование: Git Worktrees

Как улучшить систему ещё? Чтобы не ждать, пока ИИ закончит одну задачу, я использую **Git Worktrees** — позволяет создать изолированную копию проекта в отдельной папке.

Я могу запустить несколько агентов параллельно и изолированно.

### Создание worktree:

```bash
# Из папки с основным репозиторием
cd Project/
git worktree add ../Worktrees/feature-auth -b feature/add-auth
git worktree add ../Worktrees/feature-payments -b feature/add-payments
```

### Запуск параллельных агентов:

```bash
# Терминал 1
cd Worktrees/feature-auth && claude

# Терминал 2
cd Worktrees/feature-payments && claude
```

### Удаление worktree после завершения:

```bash
git worktree remove ../Worktrees/feature-auth
```

---

## Итог

Вместо того чтобы писать код руками, я занимаюсь **ревью и архитектурой**. Нейросеть пишет код, сама себя документирует и работает в несколько потоков. Мне остаётся только просмотреть код, протестировать и отправить в репозиторий.

### Планы по улучшению:

- Настроить поиск по Docs, чтобы ИИ сам находил нужные инструкции по смыслу, а не только по ключевым словам
- Подключить Jira API — тогда Claude сам скачает условия задачи и вам не придётся переносить ТЗ руками
- Объединить Gemini и Claude: первая изучит документацию, а вторая напишет на их основе чистый код

---

## Как использовать этот шаблон

```bash
# Клонируйте репозиторий
git clone https://github.com/IlyaYDen/claude-workflow-template.git

# Скопируйте в ваш проект
cp -r claude-workflow-template/{.claude,tasks,docs,CLAUDE.md} ./ваш-проект/

# Отредактируйте CLAUDE.md под ваш стек
# Добавьте документацию в docs/
# Запустите claude и начните с /init-project
```

## Файлы в репозитории

| Файл | Описание |
|------|----------|
| [`CLAUDE.md`](CLAUDE.md) | Шаблон главного промпта |
| [`.claude/commands/analyze-task.md`](.claude/commands/analyze-task.md) | Команда `/analyze-task` |
| [`.claude/commands/implement.md`](.claude/commands/implement.md) | Команда `/implement` |
| [`.claude/commands/review.md`](.claude/commands/review.md) | Команда `/review` |
| [`.claude/commands/init-project.md`](.claude/commands/init-project.md) | Команда `/init-project` |
| [`tasks/todo/example-task.md`](tasks/todo/example-task.md) | Пример оформления задачи |

---

**Как вы используете Claude Code?**

Отвечайте в комментариях в моём телеграм канале: https://t.me/ilyasitecur
